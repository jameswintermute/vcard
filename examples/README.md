# examples/

Sample `.vcf` files for testing. These are **not** processed as source contacts.

To test the pipeline, copy a file from here into `cards-in/`:

```bash
cp examples/sample.vcf cards-in/
```
