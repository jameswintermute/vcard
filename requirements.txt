# vCard Studio — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Web UI only (minimum — no Xcode / compiler needed on Mac):
#   pip install -r requirements.txt
#
# With CLI terminal interface:
#   pip install -r requirements-cli.txt
#
# Or install via pyproject.toml (recommended):
#   pip install -e .                 # web UI only
#   pip install -e ".[cli]"          # + terminal launcher
#   pip install -e ".[full]"         # everything
#
# Python 3.11+ required.  Get it from https://www.python.org/downloads/
# (Do NOT use Homebrew Python on Mac — it requires Xcode.)
# ─────────────────────────────────────────────────────────────────────────────

# Core vCard parsing (vCard 2.1 / 3.0 / 4.0)
vobject>=0.9.7

# Fuzzy duplicate detection
rapidfuzz>=3.9

# International phone number parsing, validation, and formatting
# Wraps Google's libphonenumber — optional but strongly recommended.
# Graceful fallback if not installed.
phonenumbers>=8.13
